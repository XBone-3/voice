# Services

Thin, typed wrappers around the native bridge (TurboModules / NativeModules / Event Emitters). Pass-through only — translates JS calls to bridge calls and bridge events to JS callbacks. No business logic; the bridge stays thin per ADR-008.
