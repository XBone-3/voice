// Material 3 elevation levels, in dp (maps directly to Android's `elevation`
// style property). Mode-independent — shared by light.ts and dark.ts.
export const elevation = {
  level0: 0,
  level1: 1,
  level2: 3,
  level3: 6,
  level4: 8,
  level5: 12,
} as const;
