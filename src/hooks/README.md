# Hooks

Shared custom React hooks for UI/state concerns (e.g. subscribing to a Zustand store, reading theme values). Never wraps Android business logic directly — that belongs behind `services/` and the native bridge (ADR-002).
