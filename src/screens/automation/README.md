# Automation

Rule/routine builder and management UI for the Automation Engine (Phases 073–080). Presents triggers and actions defined natively in Kotlin; the UI composes rules, it does not evaluate or execute them (ADR-002).
