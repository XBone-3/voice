# Notifications

Notification intelligence UI — summaries and reply drafts surfaced by the Kotlin `NotificationListenerService` (Phases 047–049). Renders what the Notification Engine provides; never reads notification content directly from React Native (ADR-002, PRIVACY: NON_FUNCTIONAL_REQUIREMENTS.md).
