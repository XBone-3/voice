# Settings

User-facing configuration: permissions status, privacy controls, voice/automation preferences. Presentation only — reads and writes configuration through the bridge, never touches Android APIs directly (ADR-002).
