# Assistant

The primary conversational surface — voice-first, typing as fallback (CLAUDE.md, "Interaction Hierarchy"). Displays the live conversation state driven by the Voice Engine and Command Engine; contains no recognition, parsing, or execution logic itself (ADR-002).
